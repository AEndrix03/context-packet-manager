"""Native registry client helpers for CPM builtins."""


def registry_status() -> str:
    """Return a static status to signal a healthy registry client."""

    return "registry placeholder is healthy"
