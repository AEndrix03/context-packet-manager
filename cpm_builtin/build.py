"""Placeholder build helpers for CPM builtins."""


def build_packet(name: str) -> dict[str, str]:
    """Simulate building a packet by returning a stubbed result."""

    return {"name": name, "status": "built", "feature": "build"}
